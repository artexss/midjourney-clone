const { stripIndent } = require("common-tags");
const colors = require('colors')

console.log(stripIndent`
--------------------------------------------------                                              
ARTEX & GoodBot
--------------------------------------------------
    `.red.bold)